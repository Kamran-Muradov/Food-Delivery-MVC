@model IEnumerable<CategoryVMVC>
<section class="container py-4 my-lg-3 py-sm-5" id="cuisine">
    <h2 class="text-center pt-4 pt-sm-3">Trending food in your city</h2>
    <p class="text-muted text-center mb-5">Choose what you want and we deliver it to you</p>
    <div class="row">
        @foreach (var category in Model.Take(6))
        {
            <div class="col-md-4 col-sm-6 mb-grid-gutter">
                <a class="card border-0 shadow" href="food-delivery-category.html">
                    <img class="card-img-top" src="@category.Image" alt="Burgers &amp; Fries">
                    <div class="card-body py-4 text-center">
                        <h3 class="h5 mt-1">@category.Name</h3>
                    </div>
                </a>
            </div>
        }
    </div>
</section>
